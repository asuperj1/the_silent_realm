# module/native - Archive View

This branch is derived from **main** (full archived Web version, commit bb2be22).`nIt archives ONLY the following categories:`n`n- native

> Full runnable project (all modules) is on the **main** branch.`n> Each module branch is a categorized archive view; not independently runnable.`n> Next generation (C++ / game engine / Epic) will live in a NEW repository, see MIGRATION.md on main.
